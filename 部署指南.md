# 部署上线指南

> 当前版本数据存在浏览器本地（localStorage），**不需要注册任何账号就能用**。
> 下面第一步能让它挂到网上、手机电脑都能用；后面的账号等做云同步/AI 时再弄。

---

## 🟢 第一步（现在做）：挂到 GitHub Pages —— 免费，约 10 分钟

1. 打开 https://github.com ，注册或登录（免费账号即可）。
2. 右上角 **+** → **New repository**：
   - Repository name：`timetracker`
   - 选 **Public**（公开，Pages 免费需要公开）
   - 不用勾 README，直接 **Create repository**。
3. 上传网页文件：
   - 在新仓库页面点 **uploading an existing file**（或 Add file → Upload files）。
   - 把项目里的 `index.html` 拖进去。
   - 点 **Commit changes**。
4. 开启 Pages：
   - 仓库 **Settings** → 左侧 **Pages**。
   - Source 选 **Deploy from a branch**。
   - Branch 选 **main**，文件夹选 **/(root)**，**Save**。
5. 等 1–2 分钟，访问：
   ```
   https://你的用户名.github.io/timetracker/
   ```
   - 手机浏览器打开后，用「添加到主屏幕」就像 App 一样。

> ⚠️ 注意：这一步之后，手机和电脑各自的数据仍是独立的（都存在各自浏览器里），
> 想「手机记的电脑能看到」需要下面的云同步（下一阶段做）。

### 以后更新网页
改完 `index.html` 后，回到 GitHub 仓库把新文件再 Upload 覆盖提交一次，网页几分钟后自动更新。

---

## 🟡 第二步（以后做）：云同步 / 登录 / AI —— 先别急着注册

这些等我们开始做对应功能时再一步步配置，原因：
- 现在的功能还用不到它们；
- 配置 Google 登录必须先有第一步的网址。

到时候要注册的（都免费）：

| 要注册的 | 用途 | 何时做 |
|---|---|---|
| Google Cloud OAuth 凭证 | Google 登录 + 读写你的 Google Calendar | 做登录/日历同步时 |
| Supabase 项目 | 云端存数据（多设备同步）+ AI 中转 | 做云同步时 |
| Gemini API 密钥 | AI 指令助手 | 做 AI 助手时 |

> 完整方案见 `PROJECT_PLAN.md`。
