# 时间管理 Web App — 项目方案文档

> 状态：**已上线运行** —— https://kathychen47.github.io/timetracker/
> 最后更新：2026-07-23

## ✅ 实现进度

**已完成并上线**（数据存浏览器 localStorage）：
- 完整日历：日（大类分栏×小类子栏矩阵）/周/月、现在红线、拖动建事件、重叠事件并排、勾选完成
- 两级分类（大类/小类）可自定义 + 关键词自动配色
- 番茄钟（倒计时 + 正计时，完成记入日历/统计）、待办
- 统计（可点环形图、柱状图、导出报告 CSV/复制/打印）
- 目标/计划（时长型+次数型进度条、打卡、GitHub 风格热力图详情）
- 仪表盘（迷你月历、临近截止、目标进度、备忘）
- AI 助手（Gemini，每人用自己的 key；⚠️用户免费额度为 0，暂用不了，或改 DeepSeek）
- **Google Calendar 双向**（连接/显示/推送、令牌持久化、默认同步开关）
- 部署：GitHub Pages，见 `部署指南.md`

**待做（open threads）**：
- [ ] 中英文切换（默认英文）
- [ ] 云同步（Supabase + Google 登录）——伙伴共享的地基
- [ ] 伙伴共享：看别人日程 + 留言（需上面的后端）
- [ ] AI 额度问题（Gemini 计费 或 换 DeepSeek）
- [ ] Google 日历完善（Google 侧改动回流、整天事件、后端永久连接）

---

## 一、项目目标

做一个个人时间管理网页应用，满足：

- **免费托管**：挂在 GitHub Pages 上，零成本
- **多端可用**：手机、电脑用同一个网址打开就能用，可安装到主屏幕当 App（PWA）
- **跨设备同步**：手机记的、电脑记的是同一份数据（云同步）
- **连接 Google Calendar**：与个人日历双向打通
- **有 AI 助手**：说人话就能加日程/待办、查数据，还能给洞察建议
- **数据统计可视化**：图表越直观越好
- **形成闭环**：不只是记录，而是「计划 → 执行 → 记录 → 回顾 → 调整」的完整循环，尤其是**计划 vs 实际对比**
- **多人 + 伙伴共享**：多人各用各的 Google 账号、各自私有数据；可与伙伴互看日程/数据分析并留言

---

## 二、核心理念：时间管理闭环

整个 App 围绕一个循环设计，每个功能都落在循环的某一环：

```
   ┌─────────────────────────────────────────────┐
   │                                             │
   ▼                                             │
 计划 ──→ 执行 ──→ 记录 ──→ 回顾 ──→ 调整 ────────┘
 Plan     Do      Track    Review   Adjust

 计划：时间块/日程、目标&预算、任务番茄预估
 执行：番茄钟、待办
 记录：时间记录、日历
 回顾：统计图表、计划vs实际对比、每日/周回顾、AI洞察
 调整：根据回顾修改目标与计划
```

> **计划 vs 实际对比** 是把「记录」拉回「计划」的关键一环，让循环真正转起来。

---

## 三、功能清单

### 📅 计划（Plan）

**1. 时间块 / 日程（日历视图）**
- **日 / 周 / 月三种日历视图**，展示「时间块 + Google 日历事件」
- 日视图＝时间轴，把一天规划成块，最适合"计划 vs 实际"对照
- 与 Google Calendar **双向同步**（详见模块⑫）
- 事件按颜色区分，配色规则见【🎨 日历视图 & 颜色系统】

**2. 目标 & 时间预算**
- 设周/月目标（如"本周专注 20 小时""运动 3 次"）
- 设各类别时间预算（如"工作 ≤ 40h/周""学习 ≥ 10h/周"）
- 用进度条实时追踪完成情况

**3. 任务番茄预估**
- 给待办估计需要几个番茄钟
- 与实际完成数对比（见模块⑤）

### ⚡ 执行（Do）

**4. 番茄钟 / 专注计时**
- 自定义专注时长、休息时长
- 专注 → 休息自动循环
- 结束提醒（声音/通知）
- 每完成一次专注自动记录一笔

**5. 待办清单**
- 增删改、打勾完成、优先级、截止日期
- 可"针对某个待办开始一次专注"（与番茄钟联动）
- **番茄预估 vs 实际**：估这个任务几个番茄 → 做完看实际用了几个

### 📝 记录（Track）

**6. 时间记录**
- 一键开始 / 停止计时，或手动补录一段时间
- 给时间打标签（工作 / 学习 / 运动 / 休息…），标签可自定义颜色

### 📊 回顾（Review）

**7. 统计图表（越直观越好）**
- 每日 / 每周专注时长趋势图
- 时间分布饼图（时间花在哪些标签上）
- 待办完成率
- 日历热力图（哪天专注多、哪天少）

**8. 计划 vs 实际对比 ⭐（核心视图）**
- 把「时间块/日程（计划）」和「时间记录（实际）」叠在一起看差距
- 例："计划专注 4h，实际 2.5h""计划写代码，实际在开会"
- 类别维度对比："计划学习占 30%，实际 12%"

**9. 每日 / 每周回顾**
- 一天结束花 1 分钟：给当天打分 + 记一句话
- AI 可自动生成当天/当周总结

**10. AI 周报 & 洞察**
- AI 分析一周数据给出建议（如"你周三下午效率最低，建议安排轻任务"）
- 复用 AI 指令助手模块，几乎零额外成本

### 🌱 习惯养成

**11. 习惯打卡**
- 每日打卡的习惯 + 连续天数（streak）
- 简单的打卡日历视图

### 🔗 集成 & 智能

**12. Google Calendar 双向同步**
- **读**：日历里的会议/日程显示在 App 里
- **写**：App 里排的时间块 / 完成的专注写回 Google Calendar

**13. AI 指令助手**
- 说人话，AI 翻译成操作并执行：
  - 「明天下午 3 点开会 1 小时」→ 建 Google Calendar 事件
  - 「加个待办：周五交报告」→ 建带截止日期的待办
  - 「这周我专注了几个小时？」→ 从数据算出来回答

### 🎨 日历视图 & 颜色系统

**日历视图**
- 日 / 周 / 月三种视图，展示「时间块 + Google 日历事件」
- 日视图＝时间轴，最适合"计划 vs 实际"对照

**颜色跟"类别"走**——同类同色，改一次全局生效（所有"运动"都绿、所有"开会"都蓝）

**自动配色：规则优先 + AI 兜底**
1. 先沿用 Google 日历事件**自带颜色**（如果你在 Google 那边设过）
2. **关键词规则**秒配（"会/meeting"→蓝、"健身/跑步"→绿、"学习"→紫…），快、免费、离线
3. 规则没覆盖的（如"和小王对齐需求"），交 **AI 智能分类**兜底，并**记住**结果，不重复问 AI

**手动可改**
- 点任意事件即可改颜色/类别
- 可选"以后同名都用此色"→ 沉淀成新规则，越用越准

**颜色回写 Google（用开关控制，两套都支持）**
- App 内始终用不受限的**丰富自定义色**（更好看）
- 打开开关后，把颜色**映射到 Google 最接近的预设色**写回，Google 日历 App 里也能看到颜色
- 注：Google 日历只有约 11 种预设色，所以回写是"就近取色"，App 侧更丰富

### 👥 伙伴共享（多人 + 各自私有）

- **多账号**：任意多人各用自己的 Google 登录，**各自一份私有数据**，互不干扰（免费额度支持 5 万用户）
- **配对**：通过对方 Google 邮箱发"伙伴邀请"，对方同意后建立连接
- **共享给伙伴（只读，可分项开关）**：
  - 伙伴的**日程 / 日历**：看到对方今天/本周的安排
  - 伙伴的**数据分析**：看到对方的专注时长、完成率等统计
- **隐私控制**：可把某个事件标记为"私密"，不对伙伴显示
- **留言**：伙伴之间一个消息/留言区，可互相留言（Supabase 免费支持实时更新）

> ⚠️ 配置注意：Google"测试"模式下，每个要登录的人的 Google 邮箱都需加入"测试用户"名单（上限 100，加几个邮箱即可）。

### ⚙️ 基础能力

- **Google 登录云同步**：一次登录 = 同步身份 + 日历权限；数据存云端，多设备同一份
- **PWA 离线 + 安装**：可装到手机/电脑主屏幕，没网也能用，真正像 App
- **数据导出 / 备份**：一键导出 CSV / JSON，数据安全兜底
- **提醒 / 通知**：番茄结束、待办到期、日程开始推送（*注意：手机上需装成 PWA 才可靠，iOS 有一定限制*）
- **项目 / 标签分组**：多项目时按项目聚合数据
- **明暗主题**：一键切换明亮 / 深色

---

## 四、技术架构

```
        手机 / 电脑浏览器
              │
   ┌──────────────────────────────┐
   │  GitHub Pages                 │
   │  静态网页 (React 构建, PWA)    │  ← 免费托管，可安装 + 离线
   └──────────────────────────────┘
              │
   ├─ Google 登录 ─────────→ 一次登录 = 同步身份 + 日历权限
   ├─ Supabase ───────────→ 存待办 / 番茄 / 时间记录 / 目标 / 习惯 / 回顾
   ├─ Google Calendar API ─→ 时间块双向读写
   └─ Supabase 边缘函数 ───→ 中转 Gemini AI（把密钥藏在服务器端）
```

### 技术选型
| 层 | 选型 | 说明 |
|---|---|---|
| 前端框架 | Vite + React | 功能多，用框架好维护；纯静态部署 |
| PWA | Service Worker + Manifest | 可安装、离线、通知 |
| 托管 | GitHub Pages + GitHub Actions | Actions 自动构建部署，推代码即更新 |
| 登录 | Google OAuth（经 Supabase Auth） | 一个登录解决身份 + 日历权限 |
| 数据库 | Supabase (Postgres) | 免费额度，自带 Auth |
| 日历 | Google Calendar API | 纯前端可调用 |
| AI | Google Gemini API | 免费额度，经 Supabase 边缘函数中转 |
| 图表 | 轻量图表库（待定） | 用于统计可视化 |

---

## 五、配色 & 样式

**整体风格：简约清爽**（留白多、线条干净，Notion / 苹果风）
**明暗模式：两种都做，右上角一键切换**

| 元素 | 明亮模式 | 深色模式 |
|---|---|---|
| 背景 | 白 / 极浅灰 | 深灰（不纯黑，护眼） |
| 文字 | 深灰近黑 | 浅灰白 |
| 主色 / 强调色 | **靛蓝**（默认，可改） | 同色调，稍提亮 |

**模块语义色（克制使用，只做轻点缀）：**
- 番茄钟 = 珊瑚橙　待办 = 蓝　时间记录 = 绿　日程 = 紫　目标 = 琥珀　习惯 = 青

> ⚠️ 主色默认用靛蓝，若想换成青绿或其他颜色，在开始前确定。

---

## 六、配置清单（都免费，动手前逐步操作）

| 要配置的 | 用途 | 是否花钱 |
|---|---|---|
| Google Cloud OAuth 凭证 | Google 登录 + 日历读写权限 | 免费（个人自用放"测试"模式，无需 Google 审核） |
| Supabase 项目 | 数据存储 + AI 中转边缘函数 | 免费 |
| Gemini API 密钥 | AI 指令助手 / 周报洞察 | 免费额度 |
| GitHub 仓库 + Pages | 托管网页 | 免费 |

> 具体的一步步操作指南（拿凭证、填授权网址等）在开始编码前单独提供。

---

## 七、免费额度说明

### Supabase 免费额度（个人自用绰绰有余）
| 项目 | 免费额度 | 说明 |
|---|---|---|
| 数据库 | 500 MB | 一条记录几百字节，约可存上百万条 → 够几十年 |
| 每月流量 | 5 GB | 个人用碰不到 |
| 登录用户 | 5 万 | 单人用足够 |
| 边缘函数调用 | 50 万次/月 | AI 中转足够 |
| 项目数 | 2 个 | 够 |

> ⚠️ **注意**：免费项目若连续 7 天无任何活动会被自动"暂停"（一键可恢复）。天天使用则不会触发。

### Gemini 免费额度
- 每天上千次请求的免费额度，个人使用绰绰有余。
- 密钥不写进网页源码（GitHub 仓库公开会被盗用），改由 Supabase 边缘函数在服务器端中转。

> 以上额度以各官方最新政策为准，可能会调整。

---

## 八、建设顺序（分步进行）

1. **搭骨架** — 项目脚手架 + 简约清爽 UI + 明暗切换 + PWA 基础（可安装/离线壳）
2. **早部署** — 先推上 GitHub Pages 拿到网址（手机即可打开，也为配置 OAuth 提供网址）
3. **登录 + 同步** — Google 登录 + Supabase 云同步
4. **核心模块** — 番茄钟（含番茄预估）+ 待办 + 时间记录
5. **日历** — Google Calendar 双向同步 + 日/周/月日历视图 + 颜色系统（规则+AI配色）
6. **统计** — 统计图表 + **计划 vs 实际对比** + 数据导出/备份
7. **目标 & 习惯** — 目标/时间预算 + 习惯打卡
8. **回顾 & AI** — 每日/每周回顾 + AI 指令助手 + AI 周报洞察
9. **伙伴共享** — 配对邀请 + 共享日程/数据分析 + 留言
10. **打磨** — 提醒/通知 + 项目分组 + 细节优化

---

## 九、待确认 / 可继续补充

- [ ] 主色最终定色（默认靛蓝）
- [ ] 建设顺序是否按上面来，或想优先看到某个模块
- [ ] 图表库最终选型
- [ ] 还想到别的功能随时补充

**功能已全部纳入**：计划vs实际对比、每日/周回顾、AI周报洞察、数据导出、PWA、任务番茄预估、目标&预算、习惯打卡、提醒、项目分组、日历视图&颜色系统、**伙伴共享（多人+互看+留言）** ✅

---

## 十、数据模型草图（初步，编码时细化）

```
users              —— 由 Supabase Auth (Google 登录) 管理

todos              —— id, user_id, 标题, 优先级, 截止日期, 完成状态,
                       预估番茄数, 实际番茄数, 项目id, 创建时间

pomodoro_sessions  —— id, user_id, 关联待办id, 开始时间, 时长,
                       类型(专注/休息)

time_entries       —— id, user_id, 标签id, 开始时间, 结束时间, 备注

tags / categories  —— id, user_id, 名称, 颜色, 图标
                       （类别是全局配色的基础，时间记录的标签共用这套类别）

color_rules        —— id, user_id, 关键词, 匹配到的类别id, 优先级
                       （关键词→颜色的自动配色规则，可手动增改）

projects           —— id, user_id, 名称, 颜色

goals              —— id, user_id, 类型(目标/预算), 周期(周/月),
                       目标值, 关联标签或类别, 起止时间

habits             —— id, user_id, 名称, 频率, 颜色
habit_logs         —— id, habit_id, 日期, 是否完成

reviews            —— id, user_id, 日期或周, 评分, 笔记, AI总结

settings           —— user_id, 主题(明/暗), 专注时长, 休息时长, 主色…

partnerships       —— id, 用户A, 用户B, 状态(邀请中/已连接)
sharing_settings   —— user_id, 是否共享日历, 是否共享数据分析
messages           —— id, partnership_id, 发送者, 内容, 时间（伙伴留言，可实时）

时间块 / 日程       —— 存在 Google Calendar，不单独建表（双向同步）；
                       可标记"私密"(is_private) 使其不对伙伴显示
```

---

*本文档随讨论持续更新。确认无误后再进入编码阶段。*
